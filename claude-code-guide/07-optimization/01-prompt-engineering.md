# 효과적인 프롬프트 작성법

> Claude Code에서 원하는 결과를 빠르게 얻기 위한 프롬프트 엔지니어링 가이드입니다.

---

## 1. 프롬프트 작성 원칙

### 핵심 3원칙

1. **구체적으로** (Specific): 무엇을, 어디에, 어떻게 원하는지 명확하게
2. **맥락 제공** (Contextual): 프로젝트 상황, 제약 조건, 기존 코드 설명
3. **구조화** (Structured): 요구사항을 목록이나 단계로 나누어 전달

---

## 2. Before/After 프롬프트 예제

### 예제 1: 함수 작성

**나쁜 프롬프트:**
```
정렬 함수 만들어줘
```

**좋은 프롬프트:**
```
utils/sort.ts에 배열 정렬 유틸리티 함수를 만들어줘.
- 제네릭 타입 지원
- 정렬 기준 필드와 방향(asc/desc)을 파라미터로
- 안정 정렬(stable sort) 보장
- 기존 Array.prototype.sort 래퍼가 아닌 독립 함수
```

**포인트:** 파일 위치, 기술 요구사항, 제약 조건을 명시하면 원하는 결과를 한 번에 얻을 수 있습니다.

---

### 예제 2: 버그 수정

**나쁜 프롬프트:**
```
로그인이 안 돼
```

**좋은 프롬프트:**
```
로그인 시 "401 Unauthorized" 에러가 발생해.
- POST /api/auth/login 엔드포인트
- 요청 body: { email: "test@test.com", password: "1234" }
- 서버 로그: "JWT_SECRET is undefined"
- 환경: 로컬 개발 서버 (npm run dev)
src/middleware/auth.ts 파일을 확인하고 원인을 찾아 수정해줘.
```

**포인트:** 에러 메시지, 재현 단계, 관련 파일을 제공하면 즉시 원인을 파악할 수 있습니다.

---

### 예제 3: 리팩토링

**나쁜 프롬프트:**
```
이 코드 정리해줘
```

**좋은 프롬프트:**
```
src/services/orderService.ts의 processOrder 함수를 리팩토링해줘.
- 현재 300줄 이상으로 단일 책임 원칙 위반
- 검증, 결제, 재고 차감, 알림 로직을 각각 분리
- 각 단계의 에러 핸들링 개선
- 기존 테스트가 통과하는지 확인
```

---

### 예제 4: 테스트 작성

**나쁜 프롬프트:**
```
테스트 작성해줘
```

**좋은 프롬프트:**
```
src/services/paymentService.ts의 단위 테스트를 작성해줘.
- Jest + 기존 테스트 패턴 따르기 (__tests__/ 디렉토리)
- 정상 결제, 잔액 부족, 카드 만료, 네트워크 에러 케이스
- 외부 PG사 API는 mock 처리
- 커버리지 90% 이상 목표
```

---

### 예제 5: API 설계

**나쁜 프롬프트:**
```
API 만들어줘
```

**좋은 프롬프트:**
```
상품 리뷰 REST API를 설계하고 구현해줘.
- GET /api/products/:id/reviews - 리뷰 목록 (페이지네이션, 정렬)
- POST /api/products/:id/reviews - 리뷰 작성 (인증 필요)
- PUT /api/reviews/:id - 리뷰 수정 (본인만)
- DELETE /api/reviews/:id - 리뷰 삭제 (본인 또는 관리자)
- 평점(1-5), 내용, 이미지(최대 3장) 필드
- 기존 Express + Prisma 패턴에 맞춰서
```

---

### 예제 6: 컴포넌트 생성

**나쁜 프롬프트:**
```
모달 만들어줘
```

**좋은 프롬프트:**
```
재사용 가능한 확인 다이얼로그 컴포넌트를 만들어줘.
- src/components/ConfirmDialog.tsx
- Props: title, message, confirmText, cancelText, onConfirm, onCancel, variant(danger/warning/info)
- ESC 키로 닫기, 배경 클릭으로 닫기
- 포커스 트랩 (접근성)
- 기존 디자인 시스템의 Button, Modal 컴포넌트 재사용
- Tailwind CSS 사용
```

---

### 예제 7: 데이터베이스 작업

**나쁜 프롬프트:**
```
DB 쿼리 최적화해줘
```

**좋은 프롬프트:**
```
src/repositories/productRepository.ts의 searchProducts 함수가 느려.
- 현재 응답 시간: ~3초 (10만 건 기준)
- EXPLAIN ANALYZE 결과: Seq Scan on products
- 검색 조건: name LIKE, category =, price BETWEEN
- 적절한 인덱스 추가하고 쿼리를 최적화해줘
- 목표: 200ms 이내
```

---

### 예제 8: 설정/환경 구성

**나쁜 프롬프트:**
```
Docker 설정해줘
```

**좋은 프롬프트:**
```
이 Next.js 프로젝트의 프로덕션 Docker 설정을 만들어줘.
- 멀티스테이지 빌드 (deps → build → runner)
- standalone 출력 모드 활용
- 최종 이미지 크기 200MB 이하 목표
- non-root 유저, 헬스체크 포함
- docker-compose.yml: 앱 + PostgreSQL + Redis
```

---

### 예제 9: 코드 분석

**나쁜 프롬프트:**
```
이 코드 설명해줘
```

**좋은 프롬프트:**
```
src/lib/scheduler.ts의 동작 원리를 설명해줘.
특히:
1. 태스크 큐의 우선순위 처리 방식
2. 동시성 제어 (maxConcurrency) 구현 방법
3. 재시도 로직과 백오프 전략
4. 메모리 누수 가능성이 있는 부분
각 핵심 함수별로 설명해주고, 개선점이 있으면 제안해줘.
```

---

### 예제 10: 마이그레이션

**나쁜 프롬프트:**
```
TypeScript로 바꿔줘
```

**좋은 프롬프트:**
```
src/utils/ 디렉토리의 JavaScript 파일들을 TypeScript로 마이그레이션해줘.
- .js → .ts 변환
- any 타입 사용 최소화, 적절한 타입/인터페이스 정의
- 기존 JSDoc 주석을 타입으로 변환
- 외부 라이브러리의 @types 패키지 필요시 알려줘
- 한 파일씩 순서대로 진행 (의존성 순서 고려)
```

---

## 3. 고급 프롬프트 패턴

### 단계별 지시

복잡한 작업은 단계를 나누어 지시합니다:

```
사용자 인증 시스템을 구현해줘. 다음 순서로 진행해:

1단계: Prisma 스키마에 User, Session 모델 추가
2단계: 회원가입 API (이메일 중복 검사, 비밀번호 해싱)
3단계: 로그인 API (JWT 발급)
4단계: 인증 미들웨어 (토큰 검증)
5단계: 각 단계별 테스트 작성

각 단계 완료 후 결과를 보여주고 다음으로 진행해.
```

### 제약 조건 명시

```
이 함수를 최적화해줘. 단, 다음 제약사항을 지켜줘:
- 외부 라이브러리 추가 금지
- 기존 public API 시그니처 유지
- 하위 호환성 보장
- 기존 테스트 모두 통과
```

### 출력 형식 지정

```
이 프로젝트의 의존성을 분석해줘. 결과를 다음 형식으로:

## 직접 의존성
| 패키지명 | 현재 버전 | 최신 버전 | 업데이트 긴급도 |

## 보안 취약점
| 패키지명 | 취약점 | 심각도 | 해결 방법 |

## 권장 조치
1. [즉시] ...
2. [다음 스프린트] ...
```

### 역할 지정

```
시니어 보안 엔지니어 관점에서 이 인증 코드를 리뷰해줘.
OWASP Top 10 기준으로 취약점을 찾고 심각도 순으로 정리해줘.
```

---

## 4. 피해야 할 패턴

| 피해야 할 것 | 이유 | 대안 |
|-------------|------|------|
| 너무 긴 프롬프트 (500자+) | 핵심이 흐려짐 | 요구사항을 목록으로 정리 |
| 모호한 표현 ("좋게", "깔끔하게") | 주관적 해석 | 구체적 기준 제시 |
| 한 번에 너무 많은 요청 | 품질 저하 | 단계별로 나누어 요청 |
| 파일 경로 생략 | 잘못된 위치에 생성 | 항상 경로 명시 |
| 기존 코드 패턴 무시 | 일관성 깨짐 | "기존 패턴을 따라서" 추가 |

---

## 5. 상황별 프롬프트 템플릿

### 새 기능 구현

```
[기능명]을 구현해줘.
- 위치: [파일/디렉토리 경로]
- 요구사항:
  - [요구사항 1]
  - [요구사항 2]
- 제약사항: [있다면]
- 참고: [기존 유사 코드나 패턴]
- 테스트도 함께 작성해줘.
```

### 버그 수정

```
[증상/에러 메시지]가 발생해.
- 재현 방법: [단계]
- 관련 파일: [경로]
- 에러 로그:
```
[에러 로그 붙여넣기]
```
원인을 분석하고 수정해줘.
```

### 코드 리뷰

```
[파일 경로]의 변경사항을 리뷰해줘.
- 확인 포인트: 보안, 성능, 가독성, 에러 핸들링
- [특별히 신경 쓰는 부분]에 주의해줘
```

> **다음 단계:** [CLAUDE.md 최적화](./02-claude-md.md)로 이동하여 프로젝트 설정을 최적화하세요.
